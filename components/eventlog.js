import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const EventLog = (t) => {
    return (
		<Text style={{color: 'white'}}>Events: {t}</Text>
    );
}

export default EventLog;
